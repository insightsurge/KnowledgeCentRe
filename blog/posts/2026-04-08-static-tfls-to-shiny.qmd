---
title: "From Static TFLs to Interactive Shiny Dashboards: Exploring Safety & Efficacy Dynamically"
date: "2026-04-08"
author: "Harsh Verma"
categories: [Shiny, Interactive, Future Trends, Regulatory]
description: "Are static RTF tables obsolete? How the FDA and the pharmaceutical industry are moving toward interactive submissions."
---

## The 2,000-Page PDF

If you have ever been involved in filing an NDA (New Drug Application) or BLA (Biologics License Application), you understand the sheer volume of paper (or digital paper) generated.

A regulatory submission contains thousands of static Pages—forests worth of listings, hundreds of cross-tabulated baseline characteristic tables, and endless permutations of `PROC SGPLOT` outputs. 

When an FDA statistician reviews a submission, they don't just look at the PDF. They load the provided XPT files into their own SAS or R environments and run their own analyses. Why? Because a static table can’t answer a dynamic question. 

*“I see the Kaplan-Meier curve for the overall population here on Page 45. Now I want to see this same curve, but solely for patients who were negative for this specific biomarker. Oh, that’s not in the PDF... I guess I have to program it myself.”*

## The Interactive Future: Regulatory Shiny Apps

The modern solution is deploying a validated, interactive **Shiny Application** as part of the regulatory submission itself.

The FDA has been explicitly encouraging innovation in this space through the R Consortium's Pilot Submissions project. We are reaching a point where sponsors can submit an open-source R package combined with a Shiny App wrapped in a Docker container.

### What an Interactive Efficacy App Achieves

Imagine shifting from standard TFLs to a dynamic Efficacy Explorer Dashboard. 

```{r}
#| eval: false
# A conceptual example of a custom efficacy tab in Shiny
tabPanel("Efficacy Explorer",
         sidebarLayout(
           sidebarPanel(
             selectInput("endpoint", "Select Primary Endpoint", choices = c("PFS", "OS")),
             sliderInput("age_filter", "Age Range", min = 18, max = 99, value = c(18, 99)),
             checkboxGroupInput("biomarker", "Biomarker Status", choices = c("Positive", "Negative", "Unknown"))
           ),
           mainPanel(
             plotOutput("km_plot"),      # Dynamic Survival Curve
             tableOutput("cox_model")    # Dynamic Hazard Ratio Table
           )
         )
)
```

In this paradigm:
1. **The Reviewer controls the data:** They can tweak the specific populations, endpoints, and covariates themselves.
2. **Instant answers:** Instead of issuing an Information Request (IR) back to the sponsor ("Please supply the Hazard Ratios for the XYZ subpopulation") and waiting 5 days, the reviewer moves a slider in the app and gets the answer instantly.

## The Demise of the Output Delivery System (ODS)

While we map the future of R, we have to look in the rearview mirror. 

The SAS macro library approach to generating static `.rtf` outputs is inherently limited to 2D paper matrices. We try to hack interactivity using giant hyperlinked Excel documents (`PROC EXPORT`), but it falls flat.

**Shiny Dashboards and `{teal}` represent a total paradigm shift from "Printing to Paper" to "Navigating Data."**

## How to Prepare for the Shift

If you are a clinical data programmer looking to safeguard your career over the next ten years, simply translating your `PROC REPORT` knowledge to R's `{flextable}` is not enough.

To stand out in the rapidly evolving R/Pharma landscape:
1. **Master `{admiral}`:** Understand how to build ADaMs natively in R.
2. **Master APIs & Deployment:** Learn how to host Shiny applications on Posit Connect or Shiny Server.
3. **Master Git & Version Control:** R projects are software development environments, not just linear scripts scattered in network drive folders.
4. **Learn R Packages:** Understand how to wrap your repetitive clinical derivations into `KnowledgeCentReggplots` style internal libraries.

The shift from row-by-row procedural coding to functional data pipelines has already happened. The shift from paper outputs to interactive applications is happening right now.

**Embrace the change. The tidyverse is waiting!**
